# Rally — Base44 live backend
# Copy to .env and paste your App ID from https://app.base44.com
# (your app -> Settings -> App ID). Without it the app runs on the
# offline mock store. See BACKEND.md for the required entity schema.
VITE_BASE44_APP_ID=
